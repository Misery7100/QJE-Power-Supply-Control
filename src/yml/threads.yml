timeouts:
  # timeout on "OUTPUT" press 
  # should be more than 0.2 s due PSU nature
  output_while: 0.23

  # psu read polling rate
  global_thread: 0.07
  in_step: 0.07

  # psu capture polling rate
  psu_capture: 1